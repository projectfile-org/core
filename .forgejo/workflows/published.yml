# SPDX-FileCopyrightText: 2026 Damián Búho <damian.buho@proton.me>
#
# SPDX-License-Identifier: MIT
#
# Generated by ci-resolver from org.projectfile.ci — DO NOT EDIT.
# Regenerate with: pf-ci generate --target forgejo
# A freshness gate (pf-ci generate --check) runs in CI so this file cannot
# silently drift from the source DAG.
---
name: published
"on":
  push: {}
  pull_request: {}
  workflow_dispatch:
jobs:
  artifact-ready:
    runs-on: docker
    needs: ["built"]
    steps:
      - run: echo artifact-ready
  built:
    runs-on: docker
    needs: ["source-is-ready"]
    steps:
      - run: echo built
  module-published:
    runs-on: docker
    needs: ["ready-to-publish"]
    if: startsWith(github.ref, 'refs/tags/')
    env:
      FORGEJO_TOKEN: ${{ secrets.RELEASE_TOKEN }}
    steps:
      - uses: actions/checkout@v7
      - name: publish-go-module
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.B19_DOCKER_REGISTRY }}/b19/go
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: .scripts/publish-go-module.sh "${{ github.ref_name }}"
          env: |-
            FORGEJO_TOKEN=${{ secrets.RELEASE_TOKEN }}
  published:
    runs-on: docker
    needs: ["module-published", "ready-to-publish"]
    steps:
      - run: echo published
  ready-to-publish:
    runs-on: docker
    needs: ["artifact-ready", "source-is-compliant", "source-is-secure"]
    steps:
      - run: echo ready-to-publish
  source-is-compliant:
    runs-on: docker
    steps:
      - uses: actions/checkout@v7
      - name: reuse-lint
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/python-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: reuse lint
  source-is-ready:
    runs-on: docker
    needs: ["source-is-tested", "source-passes-lint"]
    steps:
      - run: echo source-is-ready
  source-is-secure:
    runs-on: docker
    env:
      OSV_SCANNER_LOCAL_DB_CACHE_DIRECTORY: /app/.cache/osv
    steps:
      - uses: actions/checkout@v7
      - name: grype-scan-source
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/go-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-grype
          mounts: /app/data/scanner-db/grype-db:/app/.cache/grype:ro
      - name: osv-scanner-scan-source
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/go-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-osv-scanner --offline-vulnerabilities
          env: |-
            OSV_SCANNER_LOCAL_DB_CACHE_DIRECTORY=/app/.cache/osv
          mounts: /app/data/scanner-db/osv-db:/app/.cache/osv:ro
      - name: trivy-scan-source
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/go-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-trivy fs
          env: |-
            TRIVY_CACHE_BACKEND
          mounts: /app/data/scanner-db/trivy-db:/app/.cache/trivy:ro
      - uses: actions/upload-artifact@v3
        if: ${{ always() }}
        with:
          name: reports-source-is-secure-${{ github.run_id }}
          path: reports/
          if-no-files-found: warn
  source-is-tested:
    runs-on: docker
    steps:
      - uses: actions/checkout@v7
      - name: go-test
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.B19_DOCKER_REGISTRY }}/b19/go
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: go test -coverprofile=coverage.out ./...
          env: |-
            GOPROXY
  source-passes-lint:
    runs-on: docker
    steps:
      - uses: actions/checkout@v7
      - name: cffr-validate
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/r-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-cffr-validate
      - name: go-vet
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.B19_DOCKER_REGISTRY }}/b19/go
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: go vet ./...
          env: |-
            GOPROXY
      - name: golangci-lint
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/go-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-golangci-lint
          env: |-
            GOPROXY
      - name: gosec
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/go-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-gosec
          env: |-
            GOPROXY
      - name: jsonlint
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/js-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-jsonlint
      - name: shellcheck
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/misc-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-shellcheck
      - name: yamllint
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/python-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-yamllint
