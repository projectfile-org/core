# SPDX-FileCopyrightText: 2026 Damián Búho <damian.buho@proton.me>
#
# SPDX-License-Identifier: MIT
#
# Generated by ci-resolver from org.projectfile.ci — DO NOT EDIT.
# Regenerate with: pf-ci generate --target forgejo
# A freshness gate (pf-ci generate --check) runs in CI so this file cannot
# silently drift from the source DAG.
---
name: module-published
"on":
  push:
    tags: ["**"]
jobs:
  module-published:
    runs-on: docker
    needs: ["source-is-ready"]
    if: startsWith(github.ref, 'refs/tags/')
    env:
      FORGEJO_TOKEN: ${{ secrets.RELEASE_TOKEN }}
    steps:
      - uses: actions/checkout@v7
      - name: publish-go-module
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.B19_DOCKER_REGISTRY }}/b19/go
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: .scripts/publish-go-module.sh "${{ github.ref_name }}"
          env: |-
            FORGEJO_TOKEN=${{ secrets.RELEASE_TOKEN }}
  source-is-ready:
    runs-on: docker
    needs: ["source-is-tested", "source-passes-lint"]
    steps:
      - run: echo source-is-ready
  source-is-tested:
    runs-on: docker
    steps:
      - uses: actions/checkout@v7
      - name: fetch-spdx
        run: .scripts/download-spdx.sh
      - name: go-test
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.B19_DOCKER_REGISTRY }}/b19/go
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: go test -coverprofile=coverage.out ./...
          env: |-
            GOPROXY
  source-passes-lint:
    runs-on: docker
    steps:
      - uses: actions/checkout@v7
      - name: cffr-validate
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/r-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-cffr-validate
      - name: go-vet
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.B19_DOCKER_REGISTRY }}/b19/go
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: go vet ./...
          env: |-
            GOPROXY
      - name: golangci-lint
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/go-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-golangci-lint
          env: |-
            GOPROXY
      - name: gosec
        uses: projectfile/ci-actions/run-tool@v1
        with:
          image: ${{ vars.D9T_DOCKER_REGISTRY }}/d9t/go-tools
          version: ${{ vars.BASE_IMAGE_DEFAULT_VERSION || 'latest' }}
          run: auto-gosec
          env: |-
            GOPROXY
